import React from 'react';
import AdminLayout from '@/layouts/AdminLayout';
import { ClassMainContent } from '../components';

const ClassPage: React.FC = () => {
    return (
        <AdminLayout>
            <ClassMainContent />
        </AdminLayout>
    );
};

export default ClassPage;
